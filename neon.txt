#include<iostream>
using namespace std;
int main(){
  int n=9;
  int square=n*n;
  int sum=0;
  while(square>0){
    int digit=square%10;
    sum+=digit;
    square/=10;
  }
  if(sum==n)
    cout<<n<<"is a neon number";
  else
    cout<<n<<"is not a neon number";
  return 0;
}